apiVersion: v1
kind: Service

metadata:
  name: cloudforge-service

spec:
  type: NodePort

  selector:
    app: cloudforge

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
      nodePort: 30082
